<h1>New user</h1>
<%= render 'form' %>
<%= link_to 'Back', users_path %>

<%= debug(params) %>
<%= request.method_symbol.nil?? 'it is nill':request.method_symbol %>
<br />
<% @obj=request.headers %>
<% if not @obj.nil? %>
  <div>
    <% @obj.each do |key, value|%>
     <p><%= "#{key}--#{value}" %></p>
    <% end %>
  </div>
<% end %>

